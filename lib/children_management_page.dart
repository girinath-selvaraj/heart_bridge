import 'package:flutter/material.dart';

class ChildrenManagementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Children Management')),
      body: Center(child: Text('Manage child records here')),
    );
  }
}
